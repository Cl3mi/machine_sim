services:
  app:
    build:
      context: .
      network: host
    network_mode: host
    ports:
      - "3000:3000"
      - "4840:4840"
    environment:
      - PORT=3000
      - OPCUA_TCP_PORT=4840
      - OPCUA_ENDPOINT_URL=opc.wss://simulation.clxmi.com/opcua
    restart: unless-stopped
