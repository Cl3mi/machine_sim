services:
  app:
    build:
      context: .
      network: host
    network_mode: host
    ports:
      - "3000:3000"
      - "4840:4840"
    environment:
      - PORT=3000
    restart: unless-stopped
