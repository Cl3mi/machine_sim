# prometheus.yml — scrape config for the PlantSim PoC app
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: plantsim
    static_configs:
      - targets: ['localhost:3000']
    metrics_path: /metrics
